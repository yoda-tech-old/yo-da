<div class="jumbotron">
  <h1>Yo-Da</h1>
  <p class="lead">Take back control of your data</p>
  {{#if userGoogle}}
    <p>Welcome {{userGoogle}}!</p>
  {{else}}
    <a class="btn btn-primary btn-large" href="{{signInUrl}}">Login with hotmail</a>
    <a class="btn btn-primary btn-large" href="/auth/google">Login with google</a>
  {{/if}}
</div>